/**
 *
 * @param {string} mdxSource - A string of mdx source code
 * @param {import('./types').BundleMDXOptions} options
 * @returns
 */
export function bundleMDX(mdxSource: string, { files, xdmOptions, esbuildOptions, globals, cwd, }?: import('mdx-bundler/dist/types').BundleMDXOptions): Promise<{
    code: string;
    frontmatter: {
        [key: string]: any;
    };
}>;
